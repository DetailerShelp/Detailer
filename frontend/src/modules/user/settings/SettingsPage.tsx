import { PageContent } from "@/modules/user/PageContent"
import { SettingsContent } from "@/modules/user/settings/components/SettingsContent"

export const SettingsPage = () => {
  return (
    <PageContent content={<SettingsContent />} />
  )
}

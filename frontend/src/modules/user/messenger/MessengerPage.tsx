import { PageContent } from "@/modules/user/PageContent";

export const MessengerPage = () => {
  return (
    <PageContent
    // TODO
      content={<span>messenger</span>}
      widgetOne={<span>widget1</span>}
      widgetTwo={<span>widget2</span>}
    />
  )
}
